import { FlagComponent } from './flag.component';

describe('FlagDirective', () => {
  it('should create an instance', () => {
    const directive = new FlagComponent();
    expect(directive).toBeTruthy();
  });
});
